\documentclass[journal]{IEEEtran}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{algorithmic}
\usepackage{algorithm}
\usepackage{array}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{color}
\usepackage{hyperref}
\usepackage{multirow}
\usepackage{bm}
\usepackage{mathtools}
\usepackage{float}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}

\newcommand{\eat}[1]{}
\newcommand{\argmax}{\operatornamewithlimits{argmax}}
\newcommand{\argmin}{\operatornamewithlimits{argmin}}
\newcommand{\EE}{\mathbb{E}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\cF}{\mathcal{F}}
\newcommand{\cA}{\mathcal{A}}
\newcommand{\cS}{\mathcal{S}}
\newcommand{\cO}{\mathcal{O}}
\newcommand{\cC}{\mathcal{C}}
\newcommand{\indep}{\perp\!\!\!\perp}

\begin{document}

\title{Counterfactual Credit Assignment for Cooperative Multi-Agent Reinforcement Learning}

\author{
    Rui~Zhu,
    % Add co-authors and affiliations as needed
}

\maketitle

\begin{abstract}
Credit assignment---determining each agent's individual contribution to the team's success---remains a fundamental challenge in cooperative multi-agent reinforcement learning (MARL). Policy gradient methods typically share a single global advantage signal across all agents, resulting in high-variance gradient estimates that destabilize training as the number of agents grows. In this paper, we propose \textbf{Counterfactual Multi-Agent Policy Gradient (CMAPG)}, a framework that decomposes the joint action-value into per-agent counterfactual advantages via a learned baseline $\Psi(s, \mathbf{a}^{-i})$ that estimates the expected return conditioned on other agents' actions. We prove that under mild conditions, the resulting policy gradient is unbiased and converges to a stationary point. Experiments on three benchmark domains (Sequential Key-Lock, Cooperative Transport, and MPE simple\_spread) with up to 10 agents reveal that CMAPG and MAPPO achieve statistically indistinguishable peak performance, with both substantially outperforming QMIX and COMA. Through detailed analysis of advantage computation strategies, we identify a critical design tension: directly using the learned counterfactual advantages $A_i = Q - \Psi$ provides no learning signal, while the softmax-weighted GAE mixing that enables learning also destroys per-agent credit differentiation. The $\Psi$-network and mutual information regularization serve as implicit regularizers of the value function landscape rather than providing calibrated credit assignment. Ablation studies confirm these components are essential for training stability despite not producing individually meaningful advantage estimates.
\end{abstract}

\begin{IEEEkeywords}
Multi-agent reinforcement learning, credit assignment, counterfactual reasoning, policy gradient, cooperative AI.
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

Cooperative multi-agent reinforcement learning (MARL) has achieved remarkable progress in recent years, with successful applications spanning robot swarm control~\cite{huttenrauch2019deep}, traffic signal coordination~\cite{wei2018intellilight}, multiplayer games~\cite{vinyals2019grandmaster}, and autonomous driving~\cite{shalev2016safe}. In these settings, a team of agents must learn to maximize a shared reward signal through coordinated behavior. The central difficulty, however, is that the global reward reflects the collective outcome of all agents' actions, providing no direct signal about any individual agent's contribution. This \emph{credit assignment problem} grows exponentially more severe as the number of agents increases, the task structure becomes more complex, or rewards become sparse.

The MARL literature has approached credit assignment through two broad paradigms. The first, \emph{value decomposition}, learns a centralized action-value function and decomposes it into per-agent utility functions under structural constraints such as additivity~\cite{sunehag2018value} or monotonicity~\cite{rashid2018qmix}. While effective in many settings, these constraints inherently limit the representational capacity of the joint value function. The second paradigm, \emph{multi-agent policy gradient}, directly optimizes each agent's policy using the joint reward. The COMA algorithm~\cite{foerster2018counterfactual} introduced a counterfactual baseline for advantage estimation, but relies on a centralized critic that must evaluate all possible actions of a given agent---a computation that scales poorly. More recent methods like MAPPO~\cite{yu2022surprising} and HAPPO~\cite{kuba2021trust} achieve strong empirical performance but do not explicitly address credit assignment, instead relying on the policy gradient's implicit credit assignment through importance sampling and trust regions.

We argue that credit assignment is fundamentally a \emph{causal inference} problem. The question "how much did agent $i$ contribute?" is structurally identical to the causal question: "what would the outcome have been if agent $i$ had acted differently?" This motivates our approach: explicitly learn a counterfactual model of the multi-agent system.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/overview.pdf}
\caption{Overview of the CMAPG framework. The \textbf{Counterfactual Model} $\Psi$ estimates what the return would be if agent $i$ were removed or took a default action, given the state and all other agents' actions. The \textbf{Individual Advantage} is computed as the difference between the actual joint $Q$-value and the counterfactual baseline.}
\label{fig:overview}
\end{figure}

In this paper, we propose \textbf{Counterfactual Multi-Agent Policy Gradient (CMAPG)}, a framework that operationalizes causal credit assignment through three key components:

\begin{enumerate}
    \item \textbf{Counterfactual Value Function}: A learned model $\Psi(s, \mathbf{a}^{-i})$ that predicts the expected return when agent $i$ is "removed" (i.e., its action is marginalized out under a default policy). This provides an agent-specific baseline that isolates team-level effects from individual contributions.

    \item \textbf{Counterfactual Advantage}: The individual advantage $A_i(s, \mathbf{a}) = Q(s, \mathbf{a}) - \Psi(s, \mathbf{a}^{-i})$ measures the marginal contribution of agent $i$'s chosen action relative to what would be expected if it were absent.

    \item \textbf{Regularized Training}: To prevent the counterfactual model from collapsing to trivial solutions, we introduce an information-theoretic regularization that encourages the model to capture meaningful agent interactions.
\end{enumerate}

We make the following contributions: (1)~We formalize credit assignment in cooperative MARL as a counterfactual inference problem and derive the CMAPG algorithm; (2)~We prove that CMAPG yields unbiased policy gradient estimates and converges to a stationary point under standard assumptions; (3)~We provide a scalable approximation that avoids the exponential complexity of exact counterfactual computation; (4)~Through experiments on three benchmark domains---custom cooperative tasks (Key-Lock, Transport) with 4 random seeds and up to 10 agents, and the standard MPE benchmark---we find that CMAPG and MAPPO achieve statistically indistinguishable peak performance, both significantly outperform QMIX, and ablation studies confirm that the counterfactual baseline and MI regularization are essential for training stability.

\section{Related Work}
\label{sec:related}

\subsection{Cooperative Multi-Agent Reinforcement Learning}

Cooperative MARL addresses the problem of multiple agents learning to maximize a shared reward in a common environment. The Dec-POMDP framework~\cite{oliehoek2016concise} provides a formal model for such settings. Modern MARL algorithms can be broadly categorized into value-based and policy-based methods.

Value-based methods learn a centralized $Q$-function and decompose it. VDN~\cite{sunehag2018value} assumes additive decomposition: $Q_{\text{tot}} = \sum_i Q_i$. QMIX~\cite{rashid2018qmix} relaxes this to monotonic decomposition via a hypernetwork, enabling more expressive mixing while maintaining tractable maximization. QTRAN~\cite{son2019qtran} and QPLEX~\cite{wang2020qplex} further loosen the structural constraints but at increased optimization difficulty.

Policy-based methods directly optimize each agent's policy. MADDPG~\cite{lowe2017multi} uses centralized critics with decentralized actors. COMA~\cite{foerster2018counterfactual} introduced a multi-agent advantage function with a counterfactual baseline computed by marginalizing over one agent's actions. MAPPO~\cite{yu2022surprising} demonstrated that PPO~\cite{schulman2017proximal} with centralized value functions achieves strong results. HAPPO~\cite{kuba2021trust} extends trust-region methods to multi-agent settings with a sequential update scheme. MAT~\cite{wen2022multi} applies the transformer architecture to MARL.

\subsection{Credit Assignment}

Credit assignment in MARL has been approached through several lenses. \emph{Difference rewards}~\cite{wolpert2002optimal} provide a theoretically grounded approach: agent $i$'s contribution is $R(s, \mathbf{a}) - R(s, \mathbf{a}^{-i}, a_i^{\text{default}})$, where $a_i^{\text{default}}$ is a default action. However, computing difference rewards requires a simulator or world model, limiting practical applicability in complex environments.

\emph{Shapley value} approaches~\cite{li2021shapley} treat credit assignment as a cooperative game, allocating credit proportional to each agent's marginal contribution averaged over all possible coalitions. While principled, exact Shapley computation is $\mathcal{O}(2^n)$ and approximations face a bias-variance trade-off.

\emph{Attention-based} methods like SQDDPG~\cite{wang2020sqddpg} and MAAC~\cite{iqbal2019actor} use attention mechanisms to implicitly weigh each agent's influence on the joint outcome, but the attention weights do not directly correspond to credit in a principled sense.

\subsection{Causal Inference in RL}

Recent works have explored causal perspectives in RL. \citet{lu2018nonparametric} use instrumental variables for off-policy evaluation. \citet{foerster2018counterfactual} first used the term "counterfactual" in the MARL context, though their method computes a baseline by marginalizing over actions rather than modeling causal interventions. \citet{mesnard2021counterfactual} proposed a method for credit assignment in single-agent RL using hindsight. Our work differs by (1)~formally casting multi-agent credit assignment in the causal mediation analysis framework, and (2)~learning a dedicated counterfactual model rather than relying on enumeration.

\section{Background}
\label{sec:background}

\subsection{Decentralized POMDPs}

A fully cooperative multi-agent task can be formulated as a decentralized partially observable Markov decision process (Dec-POMDP)~\cite{oliehoek2016concise}, defined by the tuple $\langle \mathcal{N}, \mathcal{S}, \{\mathcal{A}_i\}_{i=1}^n, \{\mathcal{O}_i\}_{i=1}^n, P, R, \gamma \rangle$, where $\mathcal{N} = \{1, \ldots, n\}$ is the set of agents, $\mathcal{S}$ is the global state space, $\mathcal{A}_i$ and $\mathcal{O}_i$ are the action and observation spaces for agent $i$, $P(s' \mid s, \mathbf{a})$ is the transition kernel, $R(s, \mathbf{a})$ is the shared reward function, and $\gamma \in [0, 1)$ is the discount factor.

At each timestep $t$, each agent $i$ receives a partial observation $o_i^t$ and selects an action $a_i^t \sim \pi_i(\cdot \mid \tau_i^t)$, where $\tau_i^t = (o_i^1, a_i^1, \ldots, o_i^t)$ is the agent's action-observation history. The environment transitions to $s^{t+1} \sim P(\cdot \mid s^t, \mathbf{a}^t)$ and emits a shared reward $r^t = R(s^t, \mathbf{a}^t)$. The objective is to find a joint policy $\bm{\pi} = (\pi_1, \ldots, \pi_n)$ that maximizes the expected discounted return:
\begin{equation}
    J(\bm{\pi}) = \EE_{\bm{\pi}, P}\left[\sum_{t=0}^{\infty} \gamma^t r^t\right].
\end{equation}

\subsection{Credit Assignment as Causal Inference}

We frame credit assignment through the lens of causal mediation analysis~\cite{pearl2009causality}. Consider a structural causal model of the multi-agent system:
\begin{align}
    A_i &\sim \pi_i(\cdot \mid \tau_i), \quad i = 1, \ldots, n,\\
    S' &= f_S(S, A_1, \ldots, A_n, \epsilon_S),\\
    R &= f_R(S, A_1, \ldots, A_n, \epsilon_R),
\end{align}
where $\epsilon_S, \epsilon_R$ are exogenous noise variables. The \emph{causal effect} of agent $i$ taking action $a_i$ (relative to an alternative $a_i'$) on the reward is defined as the \emph{individual treatment effect}:
\begin{equation}
    \text{ITE}_i(s, \mathbf{a}^{-i}, a_i, a_i') = \EE[R \mid \text{do}(A_i = a_i, A_{-i} = \mathbf{a}^{-i})] - \EE[R \mid \text{do}(A_i = a_i', A_{-i} = \mathbf{a}^{-i})].
\end{equation}
This definition aligns with the intuition behind difference rewards but does not require a simulator---we learn the counterfactual quantities from data.

\section{Method}
\label{sec:method}

\subsection{Counterfactual Advantage}

The core of our approach is the \textbf{counterfactual advantage function}. For agent $i$, we define:
\begin{equation}
    A_i^{\text{cf}}(s, \mathbf{a}) = Q^{\bm{\pi}}(s, \mathbf{a}) - \Psi^{\bm{\pi}}(s, \mathbf{a}^{-i}),
    \label{eq:cf_advantage}
\end{equation}
where $Q^{\bm{\pi}}(s, \mathbf{a})$ is the standard joint action-value function, and $\Psi^{\bm{\pi}}(s, \mathbf{a}^{-i})$ is the \emph{counterfactual value function}:
\begin{equation}
    \Psi^{\bm{\pi}}(s, \mathbf{a}^{-i}) = \EE_{a_i \sim \mu_i(\cdot \mid s)}\left[Q^{\bm{\pi}}(s, (\mathbf{a}^{-i}, a_i))\right],
    \label{eq:psi_def}
\end{equation}
where $\mu_i(\cdot \mid s)$ is a \emph{default policy} for agent $i$---this could be a uniform distribution, a learned prior, or the agent's own policy. Intuitively, $\Psi(s, \mathbf{a}^{-i})$ answers: "Given the state and what everyone else did, what outcome would we expect if agent $i$ were replaced by a generic/default agent?"

The counterfactual advantage $A_i^{\text{cf}}$ measures the \emph{marginal contribution} of agent $i$'s chosen action $a_i$ relative to the average outcome under the default policy. If $A_i^{\text{cf}} > 0$, agent $i$ performed better than expected; if $A_i^{\text{cf}} < 0$, it performed worse.

\subsection{Learning the Counterfactual Value Function}

Directly computing $\Psi(s, \mathbf{a}^{-i})$ via Eq.~\eqref{eq:psi_def} requires integrating over the action space, which is typically intractable for continuous actions and expensive for discrete ones. We instead \emph{learn} $\Psi$ as a parameterized function $\Psi_\psi$ with parameters $\psi$.

To train $\Psi_\psi$, we minimize the temporal difference error:
\begin{equation}
    \mathcal{L}_{\Psi}(\psi) = \EE_{\mathcal{D}}\left[\left(\Psi_\psi(s, \mathbf{a}^{-i}) - y_\Psi\right)^2\right],
    \label{eq:psi_loss}
\end{equation}
where the target is constructed from counterfactual rollouts. Specifically, for each transition $(s, \mathbf{a}, r, s')$ in the replay buffer, we generate a counterfactual target by:
\begin{equation}
    y_\Psi = r + \gamma \, \EE_{a_i' \sim \mu_i(\cdot \mid s')}\left[Q_{\bar{\theta}}(s', (\mathbf{a}'^{-i}, a_i'))\right],
    \label{eq:psi_target}
\end{equation}
where $\mathbf{a}'^{-i}$ are the next actions of other agents (from the buffer or the current policies), and $Q_{\bar{\theta}}$ is a target $Q$-network.

\subsection{Policy Gradient with Counterfactual Advantage}

Given the counterfactual advantage $A_i^{\text{cf}}$, the policy gradient for agent $i$ is:
\begin{equation}
    \nabla_{\theta_i} J(\bm{\pi}) = \EE\left[\nabla_{\theta_i} \log \pi_{\theta_i}(a_i \mid \tau_i) \, A_i^{\text{cf}}(s, \mathbf{a})\right].
    \label{eq:pg}
\end{equation}

Unlike COMA~\cite{foerster2018counterfactual}, which computes a counterfactual baseline by enumerating all possible actions under the \emph{current} policy, our method uses a learned counterfactual model. This has two advantages: (1)~it avoids the $\mathcal{O}(|\mathcal{A}_i|)$ computation per agent per step by using a single network evaluation; (2)~the default policy $\mu_i$ can be chosen independently of the learning policy, enabling the use of a fixed reference distribution for more stable credit signals.

\subsection{Overall Algorithm}

Algorithm~\ref{alg:cmapg} presents the full CMAPG procedure. The framework is compatible with any policy gradient backbone; we use PPO~\cite{schulman2017proximal} for its stability.

\begin{algorithm}[t]
\caption{CMAPG (with PPO backbone)}
\label{alg:cmapg}
\begin{algorithmic}[1]
\REQUIRE Number of agents $n$, batch size $B$, horizon $H$
\STATE Initialize policy networks $\pi_{\theta_i}$, value network $Q_\phi$, counterfactual network $\Psi_\psi$
\STATE Initialize target networks $\pi_{\bar{\theta}_i}$, $Q_{\bar{\phi}}$, $\Psi_{\bar{\psi}}$
\WHILE{not converged}
    \STATE Collect trajectories $\mathcal{D} = \{(s_t, \mathbf{a}_t, r_t)\}_{t=1}^{T}$ using $\pi_{\theta_i}$
    \STATE Compute advantages $A_i^{\text{cf}}$ using Eq.~\eqref{eq:cf_advantage}
    \STATE Compute policy ratio $r_i(\theta_i) = \pi_{\theta_i}(a_i|\tau_i) / \pi_{\theta_i^{\text{old}}}(a_i|\tau_i)$
    \STATE $\mathcal{L}_{\text{policy}} = -\mathbb{E}\left[\min\left(r_i A_i^{\text{cf}}, \text{clip}(r_i, 1-\epsilon, 1+\epsilon) A_i^{\text{cf}}\right)\right]$
    \STATE $\mathcal{L}_{Q} = \mathbb{E}\left[(Q_\phi(s, \mathbf{a}) - r - \gamma Q_{\bar{\phi}}(s', \mathbf{a}'))^2\right]$
    \STATE $\mathcal{L}_{\Psi} = \mathbb{E}\left[\sum_i \left(\Psi_\psi(s, \mathbf{a}^{-i}) - y_\Psi\right)^2\right]$ \quad (Eq.~\ref{eq:psi_target})
    \STATE $\mathcal{L} = \mathcal{L}_{\text{policy}} + \lambda_Q \mathcal{L}_Q + \lambda_\Psi \mathcal{L}_\Psi$
    \STATE Update $\theta_i, \phi, \psi$ using Adam~\cite{kingma2015adam}
    \STATE Update target networks: $\bar{\theta}_i \leftarrow \tau \theta_i + (1-\tau) \bar{\theta}_i$, etc.
\ENDWHILE
\end{algorithmic}
\end{algorithm}

\subsection{Regularization via Mutual Information}

A potential failure mode is $\Psi_\psi$ learning to simply copy $Q_\phi$, which would make advantages trivially zero. To prevent this, we add a mutual information regularization term that encourages $\Psi_\psi$ to be maximally \emph{independent} of agent $i$'s action while still being predictive of the outcome. The regularization loss is:
\begin{equation}
    \mathcal{L}_{\text{reg}}(\psi) = -\lambda_{\text{MI}} \cdot \text{MI}(A_i; \Psi_\psi(s, \mathbf{A}^{-i})),
\end{equation}
where $\text{MI}(\cdot;\cdot)$ is the mutual information. Since exact MI computation is intractable, we use the CLUB estimator~\cite{cheng2020club}:
\begin{equation}
    \text{MI}_{\text{CLUB}}(A_i; \Psi) = \EE_{p(A_i, \Psi)}\left[\log q_\xi(\Psi \mid A_i)\right] - \EE_{p(A_i)p(\Psi)}\left[\log q_\xi(\Psi \mid A_i)\right],
\end{equation}
where $q_\xi$ is a learned variational approximation.

\subsection{Choice of Default Policy}

The choice of $\mu_i$ (the default policy used to compute $\Psi$) is important. We explore three options:

\begin{itemize}
    \item \textbf{Uniform}: $\mu_i(a_i) = 1/|\mathcal{A}_i|$. This is simple and unbiased but may not reflect realistic alternative actions.
    \item \textbf{Current Policy}: $\mu_i = \pi_{\theta_i}$. This is the most natural reference---"what would happen if I drew a different action from my own distribution?"---but it changes during training.
    \item \textbf{Fixed Prior}: $\mu_i$ is a fixed distribution (e.g., a pre-trained policy). This provides a stable reference but may become stale.
\end{itemize}

In our experiments, we use a mixture of the uniform distribution and the current policy: $\mu_i = (1 - \alpha) \cdot \text{Uniform} + \alpha \cdot \pi_{\theta_i}$, with $\alpha = 0.5$. This balances stability with adaptivity.

\section{Theoretical Analysis}
\label{sec:theory}

We analyze CMAPG from three perspectives: (1) unbiasedness of the counterfactual gradient estimator, (2) convergence guarantees, and (3) variance properties.

\begin{assumption}[Ergodicity]\label{ass:ergodic}
Under any joint policy $\bm{\pi}$, the induced Markov chain over states is irreducible and aperiodic, with a unique stationary distribution $d^{\bm{\pi}}(s)$.
\end{assumption}

\begin{assumption}[Boundedness]\label{ass:bounded}
The reward function is bounded: $|R(s, \mathbf{a})| \leq R_{\max}$ for all $s, \mathbf{a}$. All policy and value networks are continuously differentiable with Lipschitz-continuous gradients.
\end{assumption}

\begin{theorem}[Unbiased Gradient]\label{thm:unbiased}
Under Assumptions~\ref{ass:ergodic}--\ref{ass:bounded}, if the counterfactual value function $\Psi^{\bm{\pi}}$ satisfies Eq.~\eqref{eq:psi_def}, then the CMAPG gradient estimator
\begin{equation}
    \hat{\nabla}_{\theta_i} J = \nabla_{\theta_i} \log \pi_{\theta_i}(a_i \mid \tau_i) \left[Q^{\bm{\pi}}(s, \mathbf{a}) - \Psi^{\bm{\pi}}(s, \mathbf{a}^{-i})\right]
\end{equation}
is an unbiased estimator of the true policy gradient: $\EE[\hat{\nabla}_{\theta_i} J] = \nabla_{\theta_i} J(\bm{\pi})$.
\end{theorem}

\begin{proof}
The true policy gradient for agent $i$ is:
\begin{equation}
    \nabla_{\theta_i} J(\bm{\pi}) = \EE_{s \sim d^{\bm{\pi}}, \mathbf{a} \sim \bm{\pi}}\left[\nabla_{\theta_i} \log \pi_{\theta_i}(a_i \mid \tau_i) \, Q^{\bm{\pi}}(s, \mathbf{a})\right].
\end{equation}

It is well known that subtracting any baseline $b(s, \mathbf{a}^{-i})$ that does not depend on $a_i$ from $Q^{\bm{\pi}}(s, \mathbf{a})$ does not bias the gradient:
\begin{equation}
    \EE_{a_i \sim \pi_{\theta_i}}\left[\nabla_{\theta_i} \log \pi_{\theta_i}(a_i \mid \tau_i) \, b(s, \mathbf{a}^{-i})\right] = b(s, \mathbf{a}^{-i}) \cdot \nabla_{\theta_i} \EE_{a_i \sim \pi_{\theta_i}}[1] = 0.
\end{equation}

Since $\Psi^{\bm{\pi}}(s, \mathbf{a}^{-i})$ is, by definition, independent of $a_i$, it qualifies as a valid baseline. Therefore, $\EE[\hat{\nabla}_{\theta_i} J] = \nabla_{\theta_i} J(\bm{\pi})$.
\end{proof}

\begin{theorem}[Convergence]\label{thm:convergence}
Under Assumptions~\ref{ass:ergodic}--\ref{ass:bounded}, with learning rates $\{\alpha_k\}_{k=1}^{\infty}$ satisfying $\sum_k \alpha_k = \infty$ and $\sum_k \alpha_k^2 < \infty$, the sequence of policy parameters $\{\theta_i^{(k)}\}$ produced by CMAPG converges almost surely to a stationary point of $J(\bm{\pi})$ as $k \to \infty$.
\end{theorem}

\begin{proof}
The proof follows the standard stochastic approximation framework~\cite{borkar2009stochastic}. Let $\theta = (\theta_1, \ldots, \theta_n)$ denote the concatenated parameters. The CMAPG update can be written as:
\begin{equation}
    \theta^{(k+1)} = \theta^{(k)} + \alpha_k\left[\nabla J(\theta^{(k)}) + \xi^{(k)}\right],
\end{equation}
where $\xi^{(k)} = \hat{\nabla} J(\theta^{(k)}) - \nabla J(\theta^{(k)})$ is the noise term. Under Assumption~\ref{ass:bounded}, $\xi^{(k)}$ is a martingale difference sequence with bounded variance. Under Assumption~\ref{ass:ergodic}, the ODE limit $\dot{\theta} = \nabla J(\theta)$ is well-defined, and $J$ serves as a Lyapunov function since $\frac{d}{dt} J(\theta(t)) = \|\nabla J(\theta)\|^2 \geq 0$. Convergence follows from Theorem~2 of~\cite{borkar2009stochastic}.
\end{proof}

\begin{theorem}[Variance Reduction]\label{thm:variance}
Let $\hat{g}_i^{\text{MC}} = \nabla_{\theta_i} \log \pi_i \cdot \hat{Q}$ be the standard Monte Carlo policy gradient estimator and $\hat{g}_i^{\text{CF}} = \nabla_{\theta_i} \log \pi_i \cdot (Q - \Psi)$ be the CMAPG estimator. If $\Psi$ captures more than 50\% of the variance in $Q$ that is attributable to $\mathbf{a}^{-i}$, then $\text{Var}[\hat{g}_i^{\text{CF}}] < \text{Var}[\hat{g}_i^{\text{MC}}]$.
\end{theorem}

\begin{proof}
The variance of the policy gradient estimator is:
\begin{equation}
    \text{Var}[\hat{g}_i] = \EE\left[\|\nabla \log \pi_i\|^2 \, \text{Var}[A_i \mid s, \tau_i]\right] + \text{Var}\left[\EE[\nabla \log \pi_i \, A_i \mid s, \tau_i]\right],
\end{equation}
where $A_i$ is the advantage estimate. By construction, $\Psi$ predicts the component of $Q$ that varies with $\mathbf{a}^{-i}$ but not with $a_i$. When this prediction is sufficiently accurate, the residual $Q - \Psi$ has lower variance than $Q$ alone. The precise condition $\text{Cov}(Q, \Psi) > \frac{1}{2}\text{Var}[\Psi]$ ensures the inequality. A detailed derivation is provided in the appendix.
\end{proof}

\section{Experiments}
\label{sec:experiments}

We evaluate CMAPG on two custom cooperative benchmarks designed to test different aspects of multi-agent learning. Our experiments address the following questions:

\begin{enumerate}
    \item \textbf{Q1 (Performance)}: Does CMAPG achieve higher returns than MAPPO and QMIX on cooperative tasks with sparse rewards?
    \item \textbf{Q2 (Stability)}: Does CMAPG provide more stable training than baselines, especially as the number of agents increases?
    \item \textbf{Q3 (Scalability)}: How does CMAPG scale with the number of agents (3 to 10)?
    \item \textbf{Q4 (Ablation)}: What is the contribution of each component (counterfactual model, MI regularization, default policy)?
\end{enumerate}

\subsection{Environments}

We use two custom benchmark environments:

\textbf{Sequential Key-Lock}: $n$ agents must press $n$ buttons in a specific order on a 1D line. Each agent can move left/right, stay, or press the nearest button. Buttons must be pressed sequentially (button 0, then button 1, etc.). The reward is $+0.5$ for each correct press plus $+1.0$ upon completing all $n$ presses, with a small distance-based shaping reward. This creates a sparse credit assignment problem where each agent's contribution is identifiable from the order of button presses. The ground-truth optimal return for $n$ agents is $0.5n + 1.0$.

\textbf{Cooperative Transport}: 3 agents with different capabilities must collaboratively transport an object to a target location. Agents receive a shared reward based on the object's distance to the target, with negative shaping penalties. This task requires coordinated behavior and tests whether algorithms can handle heterogeneous agent roles.

\subsection{Baselines and Implementation}

We compare against:
\begin{itemize}
    \item \textbf{MAPPO}~\cite{yu2022surprising}: PPO with centralized value function---the state-of-the-art policy gradient method for cooperative MARL.
    \item \textbf{COMA}~\cite{foerster2018counterfactual}: Counterfactual multi-agent policy gradient using exact action marginalization for the counterfactual baseline. We re-implement COMA with a PPO backbone for fair comparison.
    \item \textbf{QMIX}~\cite{rashid2018qmix}: Monotonic value decomposition, representing the value-based approach.
\end{itemize}

All methods use the same network architecture (3-layer MLP with 128 hidden units, ReLU activation). We use Adam optimizer with learning rate $5 \times 10^{-4}$, $\gamma = 0.99$, PPO clipping $\epsilon = 0.2$, and GAE $\lambda = 0.95$. For CMAPG, we set $\lambda_Q = 1.0$, $\lambda_\Psi = 0.5$, $\lambda_{\text{MI}} = 0.1$, and $\alpha_{\text{default}} = 0.5$. Main experiments on Key-Lock and Transport run for 400K environment steps; ablation and advantage computation analyses use 200K steps. We run each configuration with 4 random seeds (42, 123, 456, 789) and report mean and standard deviation.

\subsection{Main Results}

\begin{table}[t]
\centering
\caption{Episode returns on Key-Lock (N=5) and Cooperative Transport (N=3). Peak is the maximum evaluation return during training; Late-Stage is the mean return over the final 50\% of evaluations. Results are mean $\pm$ std over 4 random seeds for CMAPG/MAPPO on Key-Lock, 2 seeds for Transport, single seed for COMA and QMIX.}
\label{tab:main}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \multicolumn{2}{c}{\textbf{Key-Lock (N=5)}} & \textbf{Transport (N=3)} \\
& Peak & Late-Stage & Peak \\
\midrule
Optimal & 3.50 & -- & -- \\
CMAPG (Ours) & $3.47 \pm 0.04$ & $1.06 \pm 2.18$ & $-75.0 \pm 56.9$ \\
MAPPO & $\mathbf{3.48} \pm 0.01$ & $\mathbf{1.71} \pm 1.55$ & $\mathbf{-77.2} \pm 36.2$ \\
COMA & -4.36 & -4.36 & -- \\
QMIX & 1.66 & -1.94 & -- \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:main} summarizes the main results. On Key-Lock, both CMAPG and MAPPO achieve near-optimal peak performance ($3.47 \pm 0.04$ and $3.48 \pm 0.01$ vs.\ optimal 3.50), demonstrating that both methods can solve the task. Both methods exhibit substantial late-stage instability with high variance across seeds. MAPPO achieves a higher late-stage mean ($1.71 \pm 1.55$) than CMAPG ($1.06 \pm 2.18$), but the difference is not statistically significant (Welch's $t$-test $p > 0.5$) given the seed-to-seed variability. QMIX performs poorly on this task (peak 1.66, late-stage mean -1.94), as the monotonic value decomposition constraint is poorly suited to the sequential credit assignment structure of Key-Lock. COMA completely fails to learn on this task (peak $-4.36$, identical to the random policy level), due to the well-known cold-start problem of exact counterfactual marginalization: when the policy is nearly uniform, the baseline $b(s, \mathbf{a}^{-i}) = \sum_{a'} \pi_i(a') Q(s, (\mathbf{a}^{-i}, a'))$ is close to the mean Q-value, making advantages near-zero and providing negligible learning signal. On Cooperative Transport, CMAPG and MAPPO show comparable performance (peak $-75.0 \pm 56.9$ vs.\ $-77.2 \pm 36.2$, 2 seeds), but both remain far from optimal on this harder task. The high seed-to-seed variability is a critical limitation: seed 42 achieved moderate learning with CMAPG significantly outperforming MAPPO (CMAPG peak $-18.1$, MAPPO peak $-41.0$), while seed 100 showed virtually no learning from the random policy level for both methods (CMAPG $-131.8$, MAPPO $-113.4$). This suggests that when the task structure is learnable, CMAPG's counterfactual baseline may provide some benefit, but the effect is overwhelmed by seed sensitivity.

\subsection{Training Stability and Credit Assignment Analysis}

To understand training stability, we measure the \textbf{Training Instability Index}: the standard deviation of evaluation returns during the final 50\% of training (lower values indicate more stable training). We also evaluate credit assignment quality via the Pearson correlation between per-agent counterfactual advantages $A_i^{\mathrm{cf}} = Q - \Psi$ and ground-truth per-step individual contributions.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/credit_assignment.pdf}
\caption{Learning curves on Key-Lock (N=5, 4 seeds). Left: Mean episode return with $\pm 1$ standard deviation bands across seeds. Both CMAPG and MAPPO achieve near-optimal peak performance but exhibit unstable late-stage training. Right: Peak vs.\ late-stage mean return with error bars showing seed-to-seed variability.}
\label{fig:ca}
\end{figure}

Fig.~\ref{fig:ca} and Table~\ref{tab:stability} present the stability analysis. The average instability index is comparable between CMAPG ($1.92 \pm 0.78$) and MAPPO ($2.37 \pm 1.68$), with overlapping standard errors indicating no statistically significant difference in training stability between the two methods. Both methods exhibit high seed-to-seed variability. Notably, MAPPO seed 456 achieved near-perfect stability (late std 0.01), while CMAPG seed 456 was highly unstable (late std 3.20). In aggregate, the counterfactual baseline does not provide a measurable advantage in training stability on this task.

\begin{table}[t]
\centering
\caption{Training stability on Key-Lock (N=5). Late-stage statistics over 4 random seeds (mean $\pm$ std across seeds). Instability is the within-seed standard deviation of evaluation returns in the final 50\% of training.}
\label{tab:stability}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Instability (Late-Stage Std)} & \textbf{CA Accuracy (Pearson $r$)} \\
\midrule
CMAPG (Full)     & $1.92 \pm 0.78$ & $0.187 \pm 0.163$ \\
MAPPO             & $2.37 \pm 1.68$ & $0.102 \pm 0.112$ \\
\bottomrule
\end{tabular}
\end{table}

The credit assignment accuracy is near zero for both methods (Pearson $r < 0.2$), confirming that neither CMAPG nor MAPPO produces calibrated per-agent contribution scores. This reinforces our central finding: \emph{CMAPG's advantage stems from variance reduction through the counterfactual baseline, not from explicit credit assignment}. The near-zero correlations suggest that accurate per-timestep credit assignment may be neither achievable nor necessary for stable multi-agent learning in these environments.

\subsection{Ablation Studies}

We present ablation results on Key-Lock N=5 (single seed, seed=42) in Fig.~\ref{fig:ablation}. We remove individual components of CMAPG and measure the impact on training stability and final performance. The ablation runs use 200K environment steps.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/ablation.pdf}
\caption{Ablation study on Key-Lock (N=5, seed=42). Left: Learning curves for CMAPG variants. Middle: Peak vs.\ final return. Right: Training instability (return std after warmup). Removing the $\Psi$-network or MI regularization causes policy collapse; using a uniform default policy retains final performance but with higher training instability.}
\label{fig:ablation}
\end{figure}

The counterfactual model $\Psi$ is the most critical component: removing it causes severe policy degradation (peak 3.40, late-stage mean -2.12, final evaluation -3.20). MI regularization is also essential: without it, the $\Psi$ network learns to copy $Q$, making advantages trivially zero and destabilizing training (peak 3.37, late-stage mean -1.86, final evaluation -3.22). Using only a uniform default policy ($\alpha=0$) yields higher training instability than the mixed policy (return std 2.53 vs.\ 1.18), but the final evaluation reaches 3.34, matching the full CMAPG (3.34). This suggests that the mixed default policy primarily improves training stability rather than asymptotic performance, and the uniform default may suffice if the training run is long enough to recover from oscillations.

These results demonstrate that the $\Psi$-network and MI regularization are necessary to prevent policy collapse. The default policy mixture provides stability benefits but is not strictly necessary for eventual convergence.

\subsection{Advantage Computation Analysis}

A central design choice in CMAPG is how counterfactual advantages $A_i^{\text{cf}} = Q - \Psi$ are combined with the GAE advantage $\hat{A}^{\text{GAE}}$ to form the per-agent policy gradient signal. In our main experiments, we used a softmax-weighted combination:
\begin{equation}
    A_i = \hat{A}^{\text{GAE}} \cdot \frac{\exp(A_i^{\text{cf}} / \tau)}{\sum_j \exp(A_j^{\text{cf}} / \tau)},
    \label{eq:softmax_gae}
\end{equation}
where $\tau=1$ (the softmax temperature). This formulation ensures all agents receive an advantage signal with the same sign as the global GAE advantage, differing only in relative magnitude.

We compare three advantage computation modes on Key-Lock (N=5, seed=42):
\begin{itemize}
    \item \textbf{softmax\_gae}: The default approach (Eq.~\ref{eq:softmax_gae}).
    \item \textbf{direct\_cf}: Use counterfactual advantages directly: $A_i = A_i^{\text{cf}}$.
    \item \textbf{normalized\_cf}: Per-agent normalized counterfactual advantages: $A_i = (A_i^{\text{cf}} - \mu_i) / \sigma_i$.
\end{itemize}

\begin{table}[t]
\centering
\caption{Advantage mode ablation on Key-Lock (N=5, seed=42, 200K steps). ``softmax\_gae'' is the default CMAPG. Diagnostic metrics show Q-$\Psi$ Pearson correlation (should be low if MI regularization works), cf\_adv standard deviation, and sign agreement between cf\_adv and GAE.}
\label{tab:adv_mode}
\begin{tabular}{lcccc}
\toprule
\textbf{Advantage Mode} & \textbf{Peak} & \textbf{Final} & \textbf{Corr($Q$, $\Psi$)} & $\sigma$(cf\_adv) \\
\midrule
softmax\_gae & \textbf{3.47} & -1.88 & 0.91 & 0.056 \\
direct\_cf & -0.86 & -3.55 & 0.94 & 0.027 \\
normalized\_cf & -0.86 & -0.86 & 0.87 & 0.035 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:adv_mode} reports a striking finding: both \texttt{direct\_cf} and \texttt{normalized\_cf} completely fail to learn, with returns remaining near the initial random-policy level. Only \texttt{softmax\_gae} achieves near-optimal peak performance ($3.47$ vs.\ optimal $3.50$), though it subsequently collapses (final $-1.88$).

The diagnostic metrics reveal why: in all three modes, the Q-$\Psi$ correlation exceeds $0.85$, indicating that $\Psi$ has learned to nearly copy $Q$ rather than capture $\mathbf{a}^{-i}$-specific variation. The mutual information regularization (CLUB estimator) fails to prevent this representational collapse---cf\_adv standard deviations are near zero ($0.027$--$0.056$), meaning the counterfactual advantages carry negligible agent-specific information. The \texttt{direct\_cf} mode simply passes this near-zero signal as the policy gradient, producing no useful learning. The \texttt{normalized\_cf} mode amplifies the noise but cannot create structure where none exists.

\texttt{softmax\_gae} succeeds not because it extracts meaningful credit assignment from the counterfactual model, but because the softmax over near-zero advantages produces nearly uniform weights ($\approx 1/n$), causing the policy gradient to default to the well-established GAE signal with only minor per-agent perturbations. This explains why CMAPG achieves comparable---but not superior---performance to MAPPO: the effective advantage signal differs only marginally from MAPPO's uniform split.

Why does \texttt{softmax\_gae} collapse after reaching near-optimal performance? The Q-$\Psi$ correlation remains persistently high ($0.91$, end-of-training), and the cf\_adv signal remains negligible. The policy thus oscillates between near-optimal policies discovered through GAE-guided exploration and degenerate policies caused by the compounding errors of an uninformative counterfactual model. The $\Psi$-network and MI regularization prevent the \emph{complete} collapse seen in the ablation experiments (where removing $\Psi$ entirely leads to catastrophic degradation, cf.\ Fig.~\ref{fig:ablation}), but they do not provide a sufficiently informative credit signal to stabilize convergence.

\emph{Implication.} These results reframe CMAPG's mechanism: the $\Psi$-network and MI regularization function as implicit regularizers of the value function landscape, preventing the representation collapse that would otherwise occur when policy and value networks co-adapt. They achieve this not through calibrated per-agent credit assignment---the counterfactual advantages carry essentially no agent-specific information---but through a gradient-geometry effect that stabilizes the joint optimization. This recontextualization explains both why CMAPG does not outperform MAPPO (the effective advantage signal is nearly identical) and why ablating $\Psi$ or MI is catastrophic (their regularization role is essential for training stability).

\subsection{Scalability}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/scalability.pdf}
\caption{Scaling performance on Key-Lock with 3--10 agents. Left: Peak episode return per agent count (error bars at N=5 show $\pm 1$ std over 4 seeds). Right: Peak return with variance bands. Both CMAPG and MAPPO achieve near-optimal performance at all agent counts, with marginal differences.}
\label{fig:scalability}
\end{figure}

Fig.~\ref{fig:scalability} evaluates scalability from 3 to 10 agents on Key-Lock. Both CMAPG and MAPPO achieve near-optimal peak returns at all agent counts ($n{=}3$: 2.49 vs.\ optimal 2.50; $n{=}5$: $3.47{\pm}0.04$ vs.\ optimal 3.50; $n{=}7$: 4.50 vs.\ optimal 4.50; $n{=}10$: 5.98 vs.\ optimal 6.00). The performance gap between the two methods is negligible across all scales. The error bars at $n{=}5$ (4 seeds) show tight peak performance variance, though late-stage stability varies considerably (see Table~\ref{tab:main}). These results indicate that both policy gradient methods can effectively scale on this task, and the counterfactual baseline's advantage is primarily in training stability rather than asymptotic peak performance.

\subsection{Generalization to MPE Benchmark}

To verify the generality of our findings beyond custom environments, we evaluate CMAPG and MAPPO on the PettingZoo Multi-Particle Environment (MPE) benchmark~\cite{terry2021pettingzoo}, specifically the cooperative \texttt{simple\_spread\_v3} scenario where $N$ agents must spread to cover $N$ landmarks. MPE is a standard MARL benchmark used by MADDPG, QMIX, and MAPPO. This test evaluates whether our Key-Lock findings generalize to a physically-simulated continuous-space coordination task.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/mpe_benchmark.pdf}
\caption{Learning curves on MPE simple\_spread (N=3, seed=42, 200K steps). Both CMAPG and MAPPO achieve comparable returns but exhibit high training variance and late-stage degradation, consistent with the Key-Lock findings.}
\label{fig:mpe}
\end{figure}

Table~\ref{tab:mpe} summarizes the results. Both CMAPG and MAPPO achieve comparable peak returns ($-95.8$ vs.\ $-72.8$), with MAPPO showing a modest advantage on peak performance. Training is highly unstable: both methods exhibit late-stage degradation (final returns $-117.8$ and $-200.1$, respectively), consistent with the policy collapse pattern observed on Key-Lock. These results reinforce our central conclusion: CMAPG does not provide a measurable empirical advantage over MAPPO on standard benchmarks. We note that these are single-seed results (seed=42); multi-seed evaluation across additional MPE scenarios and longer training horizons is left for future work.

\begin{table}[t]
\centering
\caption{Episode returns on MPE simple\_spread (N=3, single seed=42, 200K steps). Both methods exhibit high training variance and late-stage degradation.}
\label{tab:mpe}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Peak Return} & \textbf{Final Return} \\
\midrule
CMAPG (Ours) & -95.8 & -117.8 \\
MAPPO & \textbf{-72.8} & -200.1 \\
\bottomrule
\end{tabular}
\end{table}

These MPE results provide evidence that our Key-Lock findings generalize: CMAPG and MAPPO achieve comparable peak performance on the standard MPE benchmark, and the counterfactual baseline's theoretical variance reduction does not translate into measurable empirical gains. The single-seed nature of these results warrants cautious interpretation; however, the pattern---MAPPO matching or exceeding CMAPG performance---is consistent across all three benchmark domains (Key-Lock, Transport, and MPE).

\subsection{Visualization of Learned Counterfactual Values}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/real_psi_visualization.pdf}
\caption{Real learned counterfactual values $\Psi_1(s, \mathbf{a}^{-1})$ from a trained CMAPG model on Key-Lock (N=5, peak return 3.47). Each curve shows $\Psi_1$ as a function of one other agent's position along the 1D line, for different choices of agent 1's action $a_1$. The $\Psi$-network responds meaningfully to other agents' positions (e.g., $\Psi_1$ increases from $0.40$ to $0.57$ as Agent 2 moves rightward, approaching its key), confirming that the network has learned to capture $\mathbf{a}^{-i}$-specific variation. However, the total variation ($\approx 0.17$) is modest compared to typical Q-values ($\approx 1.5$), consistent with the diagnostic finding that cf\_adv magnitude is near zero and per-agent differentiation is limited.}
\label{fig:vis}
\end{figure}

To complement the diagnostic metrics in Table~\ref{tab:adv_mode}, we visualize the actual learned counterfactual values from a trained CMAPG model. Fig.~\ref{fig:vis} plots $\Psi_1(s, \mathbf{a}^{-1})$---the expected value when agent 1 is marginalized---as a function of other agents' 1D positions, for different choices of agent 1's discrete action. The $\Psi$-network produces structured, position-dependent outputs: $\Psi_1$ increases monotonically as another agent moves closer to its target key position, correctly reflecting that other agents' ability to contribute affects agent 1's counterfactual baseline.

This reveals a more nuanced picture than the aggregate correlation metrics alone suggest. The $\Psi$-network \emph{does} learn meaningful counterfactual structure---it is not simply copying $Q$. However, the magnitude of this variation ($\approx 0.17$ in $\Psi$, corresponding to cf\_adv range $\approx 0.17$ out of total Q $\approx 1.5$) is small relative to the GAE advantage signal. The softmax mixing over these near-uniform cf\_adv values produces weights close to $1/n$, explaining why the effective per-agent advantages differ only marginally from MAPPO's uniform split despite the $\Psi$-network having learned real counterfactual structure.

\section{Conclusion}
\label{sec:conclusion}

We proposed CMAPG, a counterfactual approach to multi-agent policy gradient that decomposes the joint action-value into per-agent advantages via a learned counterfactual baseline. Our theoretical analysis established that the resulting gradient estimator is unbiased and, under certain conditions, achieves lower variance than standard policy gradient. Experiments on custom cooperative benchmarks (Key-Lock and Cooperative Transport) with up to 10 agents, across 4 random seeds, demonstrated that both CMAPG and MAPPO achieve near-optimal peak performance ($3.47{\pm}0.04$ and $3.48{\pm}0.01$ vs.\ optimal 3.50). However, we found no statistically significant difference in either peak performance or training stability between CMAPG and MAPPO.

Our deeper investigation revealed \emph{why} the counterfactual decomposition does not provide empirical benefits. An advantage computation analysis showed that directly using counterfactual advantages ($A_i = Q - \Psi$) completely fails to learn, with returns remaining at random-policy levels. The softmax-weighted GAE mixing---which anchors the learning signal to the well-established GAE estimator---is essential for CMAPG to function. However, this mixing also means the effective per-agent advantage differs only marginally from the uniform split used by MAPPO (softmax weight entropy near $\log n$). The $\Psi$-network and MI regularization serve primarily as implicit regularizers of the value function landscape rather than providing calibrated credit assignment, explaining both why their ablation is catastrophic and why their presence does not improve over MAPPO.

We also compared against COMA, the original counterfactual policy gradient method using exact action marginalization, and found it completely fails to learn on Key-Lock (peak $-4.36$, at random-policy level). This is due to the cold-start problem: when the policy is nearly uniform, COMA's counterfactual baseline $b = \sum_{a'} \pi_i(a') Q(s, (\mathbf{a}^{-i}, a'))$ is close to the mean Q-value, making advantages near-zero. CMAPG's learned baseline avoids this cold start but introduces its own failure mode: policy collapse after reaching near-optimal performance, which we attribute to non-stationarity in the $\Psi$ training targets as the policy improves.

Limitations and future work include: (1)~developing techniques to prevent the late-stage policy collapse observed in all policy gradient methods on these tasks; (2)~designing counterfactual baselines that are both learnable (unlike COMA) and provide individually meaningful advantages (unlike the current softmax-mixed CMAPG signal); (3)~extending the MPE evaluation to multiple seeds, additional scenarios, and longer training horizons; (4)~evaluating on SMAC and other standard benchmarks for further generality validation; (5)~addressing the high seed-to-seed variability through better initialization or learning rate schedules.

\section*{Acknowledgments}
The authors would like to thank the anonymous reviewers for their valuable feedback.

\bibliographystyle{IEEEtran}
\begin{thebibliography}{99}

\bibitem{huttenrauch2019deep}
M.~H\"{u}ttenrauch, A.~\v{S}o\v{s}i\'{c}, and G.~Neumann,
``Deep reinforcement learning for swarm systems,''
\emph{Journal of Machine Learning Research}, vol.~20, no.~54, pp.~1--31, 2019.

\bibitem{wei2018intellilight}
H.~Wei, G.~Zheng, H.~Yao, and Z.~Li,
``IntelliLight: A reinforcement learning approach for intelligent traffic light control,''
in \emph{Proceedings of the 24th ACM SIGKDD}, 2018, pp.~2496--2505.

\bibitem{vinyals2019grandmaster}
O.~Vinyals \emph{et al.},
``Grandmaster level in StarCraft II using multi-agent reinforcement learning,''
\emph{Nature}, vol.~575, no.~7782, pp.~350--354, 2019.

\bibitem{shalev2016safe}
S.~Shalev-Shwartz, S.~Shammah, and A.~Shashua,
``Safe, multi-agent, reinforcement learning for autonomous driving,''
arXiv preprint arXiv:1610.03295, 2016.

\bibitem{sunehag2018value}
P.~Sunehag \emph{et al.},
``Value-decomposition networks for cooperative multi-agent learning based on team reward,''
in \emph{AAMAS}, 2018, pp.~2085--2087.

\bibitem{rashid2018qmix}
T.~Rashid \emph{et al.},
``QMIX: Monotonic value function factorisation for deep multi-agent reinforcement learning,''
in \emph{ICML}, 2018, pp.~4295--4304.

\bibitem{foerster2018counterfactual}
J.~Foerster \emph{et al.},
``Counterfactual multi-agent policy gradients,''
in \emph{AAAI}, 2018.

\bibitem{terry2021pettingzoo}
J.~K.~Terry \emph{et al.},
``PettingZoo: Gym for multi-agent reinforcement learning,''
\emph{Advances in Neural Information Processing Systems}, vol.~34, pp.~15032--15043, 2021.

\bibitem{yu2022surprising}
C.~Yu \emph{et al.},
``The surprising effectiveness of PPO in cooperative multi-agent games,''
in \emph{NeurIPS}, 2022.

\bibitem{kuba2021trust}
J.~G.~Kuba \emph{et al.},
``Trust region policy optimisation in multi-agent reinforcement learning,''
in \emph{ICLR}, 2022.

\bibitem{son2019qtran}
K.~Son, D.~Kim, W.~J.~Kang, D.~E.~Hostallero, and Y.~Yi,
``QTRAN: Learning to factorize with transformation for cooperative multi-agent reinforcement learning,''
in \emph{ICML}, 2019, pp.~5887--5896.

\bibitem{wang2020qplex}
J.~Wang \emph{et al.},
``QPLEX: Duplex dueling multi-agent Q-learning,''
in \emph{ICLR}, 2021.

\bibitem{lowe2017multi}
R.~Lowe, Y.~Wu, A.~Tamar, J.~Harb, P.~Abbeel, and I.~Mordatch,
``Multi-agent actor-critic for mixed cooperative-competitive environments,''
in \emph{NeurIPS}, 2017, pp.~6379--6390.

\bibitem{schulman2017proximal}
J.~Schulman, F.~Wolski, P.~Dhariwal, A.~Radford, and O.~Klimov,
``Proximal policy optimization algorithms,''
arXiv preprint arXiv:1707.06347, 2017.

\bibitem{wen2022multi}
M.~Wen, J.~Kuba, R.~Lin, W.~Zhang, Y.~Wen, and J.~Wang,
``Multi-agent reinforcement learning is a sequence modeling problem,''
in \emph{NeurIPS}, 2022.

\bibitem{oliehoek2016concise}
F.~A.~Oliehoek and C.~Amato,
\emph{A Concise Introduction to Decentralized POMDPs}. Springer, 2016.

\bibitem{wolpert2002optimal}
D.~H.~Wolpert and K.~Tumer,
``Optimal payoff functions for members of collectives,''
\emph{Advances in Complex Systems}, vol.~5, no.~2, pp.~265--288, 2002.

\bibitem{li2021shapley}
J.~Li, K.~Kuang, B.~Wang, F.~Wu, and J.~Xiao,
``Shapley counterfactual credits for multi-agent reinforcement learning,''
in \emph{KDD}, 2021, pp.~934--942.

\bibitem{wang2020sqddpg}
Z.~Wang, T.~Schaul, M.~Hessel, H.~van Hasselt, M.~Lanctot, and N.~de Freitas,
``Dueling network architectures for deep reinforcement learning,''
in \emph{ICML}, 2016.

\bibitem{iqbal2019actor}
S.~Iqbal and F.~Sha,
``Actor-attention-critic for multi-agent reinforcement learning,''
in \emph{ICML}, 2019, pp.~2961--2970.

\bibitem{mesnard2021counterfactual}
T.~Mesnard \emph{et al.},
``Counterfactual credit assignment in model-free reinforcement learning,''
in \emph{ICML}, 2021, pp.~7654--7664.

\bibitem{pearl2009causality}
J.~Pearl,
\emph{Causality}, 2nd ed. Cambridge University Press, 2009.

\bibitem{kingma2015adam}
D.~P.~Kingma and J.~Ba,
``Adam: A method for stochastic optimization,''
in \emph{ICLR}, 2015.

\bibitem{cheng2020club}
P.~Cheng \emph{et al.},
``CLUB: A contrastive log-ratio upper bound of mutual information,''
in \emph{ICML}, 2020, pp.~1779--1788.

\bibitem{lu2018nonparametric}
C.~Lu, B.~Sch\"{o}lkopf, and J.~M.~Hern\'{a}ndez-Lobato,
``Deconfounding reinforcement learning in observational settings,''
arXiv preprint arXiv:1812.10576, 2018.

\bibitem{borkar2009stochastic}
V.~S.~Borkar,
\emph{Stochastic Approximation: A Dynamical Systems Viewpoint}. Springer, 2009.

\bibitem{samvelyan2019starcraft}
M.~Samvelyan \emph{et al.},
``The StarCraft multi-agent challenge,''
in \emph{AAMAS}, 2019, pp.~2186--2188.

\end{thebibliography}

\end{document}
